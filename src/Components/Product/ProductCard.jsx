import { Card } from "antd";
const { Meta } = Card;

const ProductCard = ({ breed }) => {
  const [image, breedName, rating, price] = breed;

  return (
    <div>
     
    </div>
  );
};

export default ProductCard;
